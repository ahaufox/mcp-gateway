---
description: 用于本项目所有 AI 编码助手、专用代理、规则、技能、插件和外部工具配置的新增、更新与审查。
globs: [".agents/**/*", ".cursor/**/*", ".claude/**/*", ".lingma/**/*", ".opencode/**/*", ".trae/**/*", "AGENTS.md"]
alwaysApply: false
---

# 智能体能力治理规范

## 1. 适用范围

本规范适用于本项目中所有 AI 编码助手、专用代理、规则、技能、插件和外部工具配置的新增、更新与审查。

重点覆盖：

- Antigravity 使用的 `.agents/rules/` 与 `.agents/skills/`。
- Claude Code 使用的 `.claude/CLAUDE.md` 与 `.claude/rules/`。
- Cursor、通义灵码、Trae 等规则镜像目录。

## 2. 总体原则

- **安全优先**：任何能力扩展都不得降低鉴权、密钥、文件写入、命令执行、数据库与部署安全边界。
- **轻量优先**：优先新增少量高价值规则或代理，禁止为了“看起来完整”而创建大量空泛配置。
- **主次清晰**：`.agents/rules/` 是 Antigravity 的主要规则入口；其他工具只做必要适配，不反向覆盖主规则。
- **可验证可回滚**：任何规则更新必须说明原因、影响范围、验证方式和回滚方式。

## 3. 自我迭代与配置更新

### 必须

- 在提出自我迭代建议时，说明要解决的问题、收益、潜在风险和替代方案。
- 在修改规则或代理配置前，优先检查现有 `.agents/rules/` 是否已有相同能力。
- 对同一治理主题优先更新既有文件，避免重复创建语义相近的新规则。
- 对权限、工具、执行范围的变更保持最小化。

### 禁止

- 禁止静默扩大工具权限，例如将写入、Bash、删除、部署、推送能力改为默认允许。
- 禁止删除或弱化安全红线、鉴权要求、CI 校验要求。
- 禁止未经用户明确要求修改业务代码、数据库结构、接口契约或前端页面。
- 禁止写入密钥、令牌、个人敏感信息或环境凭据。

## 4. 技能、插件与外部工具治理

### 技能

- 技能必须服务明确的重复性任务。
- 技能输入输出必须结构化，能够说明 `status`、`message` 和 `data`。
- 技能脚本不得绕过项目权限边界或自动执行危险命令。
- **技能目录**：本项目技能集中在 `.agents/skills/` 目录，由 `.agents/skills/README.md` 维护完整索引与适用场景；该 README 通过 `infra-rules-skills-sync` 技能分发到各编辑器环境。
- **规则与技能的引用关系**：规则与技能必须互引，不得出现规则凭空要求新能力而无技能支撑、技能在无人调用的状态下长期闲置的情况。

## 5. 多工具适配边界

- Antigravity：主要读取 `.agents/rules/`，规则更新优先落在此处。
- Claude Code：通过 `.claude/CLAUDE.md` 与 `.claude/rules/` 做轻量适配，不作为主规则源。
- Cursor / 灵码 / Trae：如需同步，只保留格式适配和规则入口说明，不创建第二套相互矛盾的规范。

## 6. 变更检查清单

修改智能体配置后，必须检查：

- 是否只修改了 AI 助手配置、规则、技能或代理文件。
- 是否没有放宽 Bash、写入、删除、部署、推送等高风险权限。
- 是否没有删除安全、鉴权、迁移、CI、回滚相关约束。
- 是否没有引入密钥、令牌或个人敏感信息。
- 是否说明了变更原因、影响范围、验证方式和回滚方式。
