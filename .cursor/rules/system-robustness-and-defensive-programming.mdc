---
description: 针对前端状态同步、大模型输出容错、及多调用点测试覆盖的防御性编程约束
globs: ["**/*"]
alwaysApply: true
---

# 防御性编程与系统健壮性规范 (System Robustness & Defensive Programming)

本规范基于过往 Bug 回测（如 MVP 阶段状态机紊乱、LLM 输出解析异常、组件调用点漏测）中总结的底层教训，旨在要求开发人员在编码和测试时突破局部视野，采取防备性更强的策略。

## 1. 状态机与 URL 同步边界解耦 (UI Interaction vs. Business State)

- **场景问题**：当尝试使用 URL 作为唯一事实来源 (SSOT) 且配合自动跳转回退逻辑时，易与 UI 组件自身纯交互意图（如树形菜单的“展开/折叠”）发生严重冲突，导致穿透式跳转。
- **强制约束**：UI 交互事件（如 `onClick`）必须与核心业务流转动作（如状态重置、路由跳转）严格解耦。
  - 对于仅影响视图表现的操作（如面板展开、折叠、Tab 切换），**禁止**与 URL 参数强绑定并触发路由跳转。
  - 路由自动同步应当放置在最顶层的 Layout 或 Page 级别进行防抖处理，严禁在底层组件点击事件中触发多重重定向。

## 2. 应对大模型 (LLM) 输出的防御性编程 (Defensive Parsing against LLM)

- **场景问题**：LLM 生成的结构化或半结构化产物（如 JSON、Markdown 表格）具有一定的不确定性（如多余的换行、随机的行首空格缩进、键名大小写变化），若解析器正则过于严苛会导致整个导出或渲染流程崩溃。
- **强制约束**：对于任何来自 LLM 的产物，必须默认其为“脏数据”并实行宽泛的容错处理。
  - **表格/正则解析**：在进行正则匹配或分词切割前，必须对行首行尾不可见字符（`\s+`, `\t`）进行 trim。正则表达式必须具备宽容度（使用 `*` 替代 `+` 匹配边界，兼容 Markdown 转义）。
  - **JSON 解析**：在 `JSON.parse()` 外必须包裹 `try-catch`，并提供 `fallback` 默认值或部分渲染策略，严禁出现因单节点解析失败导致的页面白屏。

## 3. 全局视野下的调用点审计与端到端覆盖 (Holistic Coverage & Call-Site Audit)

- **场景问题**：开发者习惯于仅在其所见的主渲染路径上修改并断言，忽视了同一组件在只读预览、弹窗等其他引用（Call-Site）下的参数透传，单测即便全绿也会在真实环境崩溃。
- **强制约束**：
  - **多调用点审查**：如组件的 Props 签名发生变化，**必须使用全库检索（如 `rg`）**审查所有 `<ComponentName` 的调用点是否遗漏。
  - **主路径优先测试**：单元与集成测试必须用特征值精准定位“业务主干”的渲染实例（Primary Path），次要路径作为补充覆盖。
  - **脱离单测依赖**：所有涉及跨组件的流转（如“新建-加载-提交-展示”）在单测联调通过后，必须具备至少一个端到端测试用例，填补单纯接口 Mock 下的盲区。

## 4. 重构与变量覆盖审查 (Refactoring & Variable Shadowing Audit)

- **场景问题**：在修复状态推导或核心业务逻辑时，开发者新增了正确的预计算逻辑（如基于拓扑图预计算子节点状态），但在同一个庞大函数的后半部分，遗漏了删除旧版的“兜底/覆盖”逻辑。这导致新计算的正确状态在返回前被旧逻辑重新覆盖为错误状态，造成“改了等于没改”的假象。
- **强制约束**：
  - **彻底清理旧逻辑**：在引入新的状态计算流或数据流时，必须全局搜索该变量在当前函数及作用域中的所有下游赋值点（Assignments）。严禁遗留会无条件覆盖新计算结果的历史残余代码（Dead Code/Shadowing）。
  - **单一事实来源赋值**：业务状态在一个处理环节中应当只被赋值一次。如果必须进行后置校验或兜底，必须显式检查该状态是否为空，而非无差别覆盖。
