---
description: 项目规则: testing-tdd-skills-standards
globs: ["mcp-proxy/**/*", "web/src/**/*.ts", "web/src/**/*.tsx"]
alwaysApply: true
---

# TDD 与测试技能规范 (TDD & Testing Skills Standards)

## 1. TDD 工作流强制规范

### 1.1 TDD Skill 加载

- **强制加载**：所有编码任务必须加载 `tdd` 技能。
- **Red-Green-Refactor**：严格遵循三步循环：
  1. **Red**：编写失败的测试
  1. **Green**：编写最小代码使测试通过
  1. **Refactor**：重构代码保持测试通过
- **实施方案文档**：每个任务必须创建实施方案文档。

### 1.2 测试金字塔

```text
        /\
       /  \  端到端测试 (E2E) - 10-20%
      /    \  - 验证完整用户流程
     /------\  - 慢速、脆弱、昂贵
    /        \
   /  集成测试  \  - 20-30%
  /            \  - 验证模块间交互
 /--------------\
|    单元测试     |  - 50-70%
|   - 快速、稳定  |  - 验证单个函数/类
```text

---

## 2. 测试替身使用规范

### 2.1 选择指南
| 场景 | 推荐类型 | 原因 |
|------|----------|------|
| 简单返回值 | **Stub** | 不需要验证交互 |
| 验证方法调用 | **Mock** | 需要验证调用次数、参数 |
| 记录调用历史 | **Spy** | 需要分析调用模式 |
| 需要真实逻辑 | **Fake** | 需要轻量级真实实现 |
| 填充参数 | **Dummy** | 不会被实际使用 |

### 2.2 强制规范
- **Fake 优先**：优先使用 Fake（简化实现）而非 Mock。
- **Mock 边界**：仅在系统边界（接口、外部 API、文件系统、数据库）使用 Mock。
- **验证行为**：Mock 应验证方法调用和参数，而非内部状态。
- **避免过度 Mock**：不要 Mock 所有依赖，保持测试真实性。

---

## 3. 属性测试规范

### 3.1 常用属性
- **幂等性**：多次执行结果相同
- **交换律/结合律**：数学性质
- **逆运算**：编码/解码、序列化/反序列化
- **不变量**：状态变化中的不变条件
- **后置条件**：操作后的预期状态

### 3.2 工具选择
- **Python**：使用 Hypothesis
- **TypeScript/JavaScript**：使用 fast-check

---

## 4. 测试最佳实践

### 4.1 AAA 模式
- **Arrange**：设置测试环境和数据
- **Act**：执行被测试的操作
- **Assert**：验证预期结果

### 4.2 测试隔离
- 每个测试独立运行，不依赖其他测试。
- 测试结果一致，不依赖外部状态。
- 单元测试应在毫秒级完成。

### 4.3 测试执行频率与范围约束
- **避免频繁全量测试**：在开发过程中，不要频繁执行全量测试套件或一大堆测试文件。只针对当前正在修改的具体文件、特定类或函数运行有限的关联测试。
- **局部运行优先**：严格限制测试范围。
- **全量测试时机**：全量测试仅在功能完全开发完毕并准备提交前执行一次。

### 4.4 测试必须覆盖主路径而非首个可见路径 (Primary Path Test Coverage)
- 当同一组件在页面中被渲染多次时，测试**必须优先覆盖主路径**（业务核心渲染实例），次要路径作为补充覆盖。
- 测试断言中必须使用**可区分不同渲染实例的特征值**（如 `data-testid`、`id`）精准定位被测实例，而非依赖调用顺序。
