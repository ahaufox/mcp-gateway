### 📅 AI 进化简报 [2026-02-13]

- **🔍 存量判定结果**:
  - 针对 [Backend Tech Stack Standardization], 发现 [存在] `PyMCPAutoGUI` (Python) 和 `mcp-server-chart` (TypeScript) 分别使用了明确的技术栈，但缺乏统一的文档记录。
  - 处置建议：[新增] 相应的规则文件以显性化这些非成文共识。

- **🔄 优化资产**:
  - 无直接修改现有资产，而是通过新增规则来增强整体架构的清晰度。

- **🆕 新增资产**:
  - **[NEW] Rule: python-modern-stack.md**: 规范 Python 项目使用 `uv` 管理依赖，推荐 `flake8`与 `pytest`，强调类型提示。
  - **[NEW] Rule: typescript-modern-stack.md**: 规范 TypeScript 项目使用 `biome` 进行代码质量控制，推荐 `vitest` 进行测试。
  - **[NEW] Rule: infra/docker-best-practices.md**: 填补了 `infra` 类别的空白，建立 Docker 镜像构建的安全与优化标准（多阶段构建、非 root 用户）。

- **💡 结构化建议**:
  - 针对最近的 [Infrastructure Awareness], 建议下一步 [审查所有 Dockerfile 是否符合新建立的最佳实践，并逐步迁移旧项目]。
  - 建议在未来引入 CI/CD 相关的 `infra` 规则。
